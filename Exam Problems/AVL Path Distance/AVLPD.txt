The longest path is the height of the tree, therefore it is log2(n) from root to leaf
AVL trees can only have a balance factor of 1 among each node, therefore the shortest path is only
    1 less than the longest path, making it log2(n)-1